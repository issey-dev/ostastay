# Building a property website on Uppsolut Stay

This guide moved into the documentation portal:

- **For administrators** — creating keys and webhooks: `/docs/configuration/enterprise/booking-api`;
  choosing what each property sells online: `/docs/configuration/property/online-booking`
- **For front office and spa staff** — online bookings at the desk: `/docs/operations/front-desk`
- **For web developers** — `/docs/api`, starting at *Getting started*.

The portal is served by the application itself (source in `src/app/docs`) and is public.
