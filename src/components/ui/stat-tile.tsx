// The ONE KPI card (DESKTOP_PLAN D8 — there were seven). It lives with the dashboard's other
// tiles; import it from here in every other screen so there is a single implementation.
export { StatTile, type StatTileProps } from "@/components/dashboard/tiles"
